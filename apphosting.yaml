# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value to handle more concurrent users. Min 2 for high availability.
  maxInstances: 4
  minInstances: 1
  concurrency: 80
  cpu: 1
  memoryMiB: 512
